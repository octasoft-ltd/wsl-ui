[package]
name = "wsl-core"
version = "0.8.0"
edition = "2021"
description = "Core WSL management logic - parsing and types"
license = "BUSL-1.1"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
# No additional dev dependencies needed

